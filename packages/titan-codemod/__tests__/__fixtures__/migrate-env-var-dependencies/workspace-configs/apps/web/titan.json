{
  "$schema": "https://titan.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      // old
      "dependsOn": ["build", "$ENV_2"],
      // new
      "env": ["ENV_1"]
    }
  }
}
